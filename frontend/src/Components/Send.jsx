import React from "react";
import Button from "./Button";
import axios from "axios";

const Send = ({ id }) => {
  console.log(id);

  async function onClick() {}
  return <div>Send</div>;
};

export default Send;
